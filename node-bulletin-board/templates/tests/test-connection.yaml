apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-bulletin-board.fullname" . }}-test-connection"
  labels:
    {{- include "node-bulletin-board.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-bulletin-board.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
